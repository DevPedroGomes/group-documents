# Core
fastapi==0.140.0
email-validator>=2.0.0
uvicorn[standard]==0.51.0
python-multipart==0.0.9
sqlalchemy==2.0.51
psycopg2-binary==2.9.12
pgvector==0.5.0
pydantic==2.13.4
pydantic-settings>=2.0.0
python-dotenv==1.0.1
httpx>=0.24,<1.0

# Auth (local JWT)
python-jose[cryptography]>=3.3.0
# passlib removed - using bcrypt directly
bcrypt>=4.0.0

# LLM & RAG
anthropic>=0.40.0
openai>=1.50.0
voyageai>=0.3.0
cohere>=5.0
tiktoken>=0.7.0

# Nucleo compartilhado do portfolio: teto de gasto, trilha de decisao e fila.
# Pinado por tag: um `main` que anda sozinho quebraria producao sem commit aqui.
agent-ops @ git+https://github.com/DevPedroGomes/agent-ops.git@v0.2.0

# Cache
redis>=5.0

# Rate limiting
slowapi>=0.1.9
limits>=3.0

# Web Search Fallback
tavily-python>=0.5.0

# Ingestion
pypdf==6.14.2
# Render de pagina escaneada (caminho visual) + manipulacao de imagem para o
# embedding multimodal. Substituem o google-generativeai, que saiu.
PyMuPDF>=1.24
Pillow>=10.0
python-magic==0.4.27
beautifulsoup4>=4.12,<5

